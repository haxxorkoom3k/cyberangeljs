Вывод текста:
    Console.log(текст в кавычках или математическое действие);

Кодировка:
    JS можно попросить вывести любой символ из ASCII. Пример - console.log(String.fromCharCode(63));
    вывести можно что угодно, находящееся в таблице кодов ASCII. (dec / decimal)

Переменная:
    задаётся тегом let. этот тег используется только при создании переменной. при её изменении его писать не нужно.
        Пример:

            let greeting = 'Father!';
            console.log(greeting);

            greeting = 'Mother!';
            console.log(greeting);

        ещё пример:

            let dollarsCount = eurosCount * 1.25;
            let rublesCount = dollarsCount * 60;
            
            console.log(dollarsCount);
            console.log(rublesCount);

TBC
